<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Показ уведомлений-напоминаний (запрашивается в рантайме на Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Голосовой ввод в ИИ-коуче -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:name=".WordTrainerApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:localeConfig="@xml/locales_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.WordTrainerApp">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settings.LanguagesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.detail.WordDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.dictionary.DictionaryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.achievements.AchievementsActivity"
            android:exported="false" />

        <!-- ИИ-коуч: чат, уроки-диалоги и «собери предложение» -->
        <activity
            android:name=".ui.coach.CoachActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Авто-сохранение выбранного языка интерфейса (AppCompat) -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>

</manifest>
