<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Показ уведомлений-напоминаний (запрашивается в рантайме на Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:name=".WordTrainerApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.WordTrainerApp">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settings.LanguagesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.detail.WordDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.dictionary.DictionaryActivity"
            android:exported="false" />
    </application>

</manifest>
